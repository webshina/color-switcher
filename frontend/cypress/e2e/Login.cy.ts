/// <reference types="cypress" />

describe('Login Test', () => {
  before(() => {});

  beforeEach(() => {});

  context('Validation', () => {
    it('Can not login with wrong password', () => {});
  });
});
